# Code Citations

## License: GPL-3.0
<https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx>

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.
```

## License: GPL-3.0
<https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx>

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.
```

## License: GPL-3.0
<https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx>

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.
```

## License: GPL-3.0
<https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx>

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.
```

## License: GPL-3.0
<https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx>

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.
```

## License: GPL-3.0
<https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx>

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.
```

## License: GPL-3.0
<https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx>

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.
```

## License: GPL-3.0
<https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx>

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.
```

## License: GPL-3.0
<https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx>

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.
```

## License: GPL-3.0
<https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx>

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.inset};
  display: inline-block;
  border-radius
```

## License: GPL-3.0
<https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx>

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.inset};
  display: inline-block;
  border-radius
```

## License: GPL-3.0
<https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx>

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.inset};
  display: inline-block;
  border-radius: 50px;
  position: relative;
  transition: all
```

## License: GPL-3.0
<https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx>

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.inset};
  display: inline-block;
  border-radius: 50px;
  position: relative;
  transition: all
```

## License: GPL-3.0
<https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx>

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.inset};
  display: inline-block;
  border-radius: 50px;
  position: relative;
  transition: all 0.3s ease;
```

## License: GPL-3.0
<https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx>

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.inset};
  display: inline-block;
  border-radius: 50px;
  position: relative;
  transition: all 0.3s ease;
  transform-origin: 20% center;
  cursor: pointer;
```

## License: GPL-3.0
<https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx>

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.inset};
  display: inline-block;
  border-radius: 50px;
  position: relative;
  transition: all 0.3s ease;
  transform-origin: 20% center;
  cursor: pointer;
`;
````

## Cosa

```


## License: GPL-3.0
https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx

```

Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.inset};
  display: inline-block;
  border-radius: 50px;
  position: relative;
  transition: all 0.3s ease;
  transform-origin: 20% center;
  cursor: pointer;
`;
````

## Cosa è cambiato

1. **Aggiunto `export`** - Necessario perché `SarcoinLabel` viene importato in `SarcoinToggle.tsx`
2. **Aggiunto `;`** alla fine - Richiesto dalla sintassi TypeScript per le dichiarazioni di variabili/costanti

## Nota aggiuntiva

Ho notato che nel file completo c'è anche `PancakeInput` invece di `SarcoinInput`. Potrebbe essere necessario rinominare:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinInput = styled.input<InputProps>`
  height: 40px;
  left: 0;
  opacity: 0;
  position: absolute;
  top: 0;
  width: 40px;

  &:focus + label {
    box-shadow: ${({ theme }) => theme.shadows.focus};
  }

  &:checked + label .sar
```


## License: GPL-3.0
https://github.com/pancakeswap/pancake-frontend/blob/97d0e5b165c0ffdf9902dd586e4038d524015ecb/packages/uikit/src/components/PancakeToggle/StyledPancakeToggle.tsx

```
Il problema è che manca un **punto e virgola** alla fine della dichiarazione di `SarcoinLabel`.

## Soluzione

Aggiungi il punto e virgola alla fine:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinLabel = styled.label<SarcoinToggleProps>`
  width: ${getScale("toggleWidth")};
  height: ${getScale("toggleHeight")};
  background: ${({ theme, checked }) => theme.colors[checked ? "success" : "input"]};
  box-shadow: ${({ theme }) => theme.shadows.inset};
  display: inline-block;
  border-radius: 50px;
  position: relative;
  transition: all 0.3s ease;
  transform-origin: 20% center;
  cursor: pointer;
`;
````

## Cosa è cambiato

1. **Aggiunto `export`** - Necessario perché `SarcoinLabel` viene importato in `SarcoinToggle.tsx`
2. **Aggiunto `;`** alla fine - Richiesto dalla sintassi TypeScript per le dichiarazioni di variabili/costanti

## Nota aggiuntiva

Ho notato che nel file completo c'è anche `PancakeInput` invece di `SarcoinInput`. Potrebbe essere necessario rinominare:

````typescript
// filepath: StyledSarcoinToggle.tsx
export const SarcoinInput = styled.input<InputProps>`
  height: 40px;
  left: 0;
  opacity: 0;
  position: absolute;
  top: 0;
  width: 40px;

  &:focus + label {
    box-shadow: ${({ theme }) => theme.shadows.focus};
  }

  &:checked + label .sar
```

